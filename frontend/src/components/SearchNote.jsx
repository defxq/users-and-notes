import React from "react";

const SearchNote = () => {
  return (
    <div className="search-note">
      <input
        type="text"
        placeholder="Search notes..."
      />
    </div>
  );
};

export default SearchNote;
